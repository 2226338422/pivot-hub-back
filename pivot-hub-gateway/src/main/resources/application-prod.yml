spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        password: nacos
      config:
        server-addr: localhost:8848
        password: nacos
  config:
    import:
      - optional:nacos:${spring.application.name}-${spring.profiles.active}.yaml
